# HTML
-- Body
    -- high scores button in upper left
    -- timer in upper right
    -- header section with game name.
    -- container container for game start that includes:
        -- Intro text
            -- Rules text
        -- Start button

    -- container for questions and answers:
        -- question text
        -- buttons that each contain answers
        -- correct/incorrect notification text

    -- container for "all done"
        -- show score summary
        -- present form/input for entering user initials
        -- show submit button

    -- container for "high scores"
        -- show list of high scores
        -- button to reset high scores
        -- button to play again




